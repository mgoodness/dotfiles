[env]
CLOUDSDK_ACTIVE_CONFIG_NAME = "mlb"
GH_HOST = "github.com"
# See dot_config/mise/config.toml.tmpl for why this exec() isn't redundant
# with fnox's own per-directory activation.
GH_TOKEN = { value = "{{ exec(cache_duration='7d', cache_key='emu-gh-token', command='/opt/homebrew/bin/fnox get GH_TOKEN') }}" }

[tools]
terraform = "latest"
terraform-docs = "latest"
