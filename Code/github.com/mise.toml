[env]
GH_HOST = "github.com"
GH_TOKEN = { value = "{{ exec(cache_duration='1d', cache_key='personal-gh-token', command='op read --account my.1password.com \"op://Private/GH CLI PAT/credential\"') }}" }
