Prioritize fff (`find_files`, `grep`, `multi_grep`) over built-in Grep/Glob and Bash grep/find for file search and content search in the current git-indexed directory.

Prefer `/spin-up-worktrees` over `/worktrunk:wt-switch-create` for starting new or parallel work. Only use `wt-switch-create` when explicitly asked to redirect the _current_ session into a new worktree in place.
